# 数据库配置示例文件
# 复制此文件为 .env 并根据实际情况修改

# 数据库类型: mysql 或 sqlite（默认已改为mysql）
DATABASE_TYPE=mysql

# MySQL配置 (默认配置，适用于本地开发)
MYSQL_HOST=localhost
MYSQL_PORT=3306
MYSQL_USER=root
MYSQL_PASSWORD=123456
MYSQL_DATABASE=automation

# 说明:
# 1. MySQL模式（推荐）: 使用上述MySQL配置，适合生产环境
# 2. SQLite模式: 设置 DATABASE_TYPE=sqlite，其他MySQL相关变量可忽略
# 3. 项目已默认使用MySQL，首次运行前请确保：
#    - MySQL服务已启动
#    - 运行 python init_mysql_database.py 初始化数据库
# 4. 修改配置后需要重启应用才能生效 